package com.iotlambda.analytics.speed.countbydevice

private[countbydevice] case class CountByType(
                        deviceType: String,
                        count: Long
                      )
